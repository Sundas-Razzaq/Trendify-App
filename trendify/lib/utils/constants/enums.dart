// list of enums
//they cannot be created inside the class

enum TextSizes { small, medium, large }

enum OrderStatus { preprocessing, shipped, delivered }

enum PaymentMethod {
  paypal,
  googlePay,
  applePay,
  visa,
  masterCard,
  creditCard,
  paystack,
  razorPay,
  paytm,
}
