import 'package:flutter/material.dart';

class SellerManageProducts extends StatelessWidget {
  const SellerManageProducts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Seller - Manage Products'));
  }
}
