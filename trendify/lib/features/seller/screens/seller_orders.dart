import 'package:flutter/material.dart';

class SellerOrders extends StatelessWidget {
  const SellerOrders({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Seller Orders'));
  }
}
