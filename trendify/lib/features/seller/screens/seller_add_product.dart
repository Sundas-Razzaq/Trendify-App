import 'package:flutter/material.dart';

class SellerAddProduct extends StatelessWidget {
  const SellerAddProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Seller - Add Product'));
  }
}
