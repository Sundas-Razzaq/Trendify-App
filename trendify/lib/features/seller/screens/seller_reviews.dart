import 'package:flutter/material.dart';

class SellerReviews extends StatelessWidget {
  const SellerReviews({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Seller - Reviews'));
  }
}
